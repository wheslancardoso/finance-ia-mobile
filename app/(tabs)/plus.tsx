import { View } from 'react-native';

/**
 * Placeholder vazio para a tab central "+" que é controlada pelo modal global
 * no _layout.tsx. Essa tela nunca é renderizada diretamente.
 */
export default function PlusPlaceholder() {
  return <View />;
}
